#ifndef CONFIG_H
#define CONFIG_H

/**
 * @brief
 * Klasa przechowujaca komplet ustawien wybranych przez uzytkownika na ekranie
 * startowym.
 */
struct Config
{
    int map_width;
    int map_height;
};